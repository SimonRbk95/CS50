cs50
Flask
Flask-Session
requests

CREATE TABLE portfolio(
   user_id INTEGER,
    stock
   FOREIGN KEY (user_id)
      REFERENCES users (id)
         ON DELETE CASCADE
         ON UPDATE NO ACTION
);